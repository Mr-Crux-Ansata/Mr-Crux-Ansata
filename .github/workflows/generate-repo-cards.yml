name: Generate Repository Cards

on:
  workflow_dispatch:
  schedule:
    - cron: "0 0 * * *"

jobs:
  generate-card:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Generate CVE repository card
        uses: stats-organization/github-readme-stats-action@v2
        with:
          card: pin
          options: username=Mr-Crux-Ansata&repo=cve-vulnerability-classifier&title_color=FF4F9A&text_color=FFF0F6&icon_color=FF9FC2&bg_color=211923&border_color=FF4F9A&hide_border=false&border_radius=6&show_owner=false
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Commit card
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add assets/cve-vulnerability-classifier.svg

          git diff --cached --quiet || git commit -m "Update repository card"

          git push
